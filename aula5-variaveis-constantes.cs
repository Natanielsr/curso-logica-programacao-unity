using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int idade; // Declaração de uma variável inteira chamada "idade"
        idade = 25; // Atribuição de um valor à variável
        print(idade); //exibe o valor da variavel idade no console

        const float pi = 3.14f; // Declaração de uma constante float chamada "pi"
        print(pi); //exibe o valor da constante pi no console

    }

}
